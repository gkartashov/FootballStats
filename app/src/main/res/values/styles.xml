<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryTextColor</item>
        <item name="android:statusBarColor">@color/primaryDarkColor</item>
    </style>

    <style name="AppTheme.LauncherTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>
    <style name="RecyclerViewItem">
        <item name="android:backgroundTint">@color/listItemsColor</item>
    </style>
    <style name="ToolBarStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/primaryColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="android:textColorSecondary">@color/primaryTextColor</item>
    </style>
    <style name="WagerTabStyle" parent="AppTheme">
        <item name="android:colorAccent">#FF0000</item>
    </style>
    <style name="OddTabStyle" parent="AppTheme">
        <item name="android:colorPrimary">#00FF66</item>
    </style>
</resources>
